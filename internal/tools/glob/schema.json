{
    "type": "object",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "required": ["path", "pattern"],
    "properties": {
        "path": {
            "type": ["string", "null"],
            "description": "The absolute directory path to search in. Use null for the tool's configured working directory. Matching file paths are returned as absolute paths."
        },
        "pattern": {
            "type": "string",
            "minLength": 1,
            "description": "The glob pattern to match files against. Supports recursive ** patterns such as **/*.go and src/**/*.ts."
        }
    },
    "additionalProperties": false
}
