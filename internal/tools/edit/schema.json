{
    "type": "object",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "required": [
      "file_path",
      "old_string",
      "new_string",
      "replace_all"
    ],
    "properties": {
      "file_path": {
        "type": "string",
        "description": "The absolute path to the file to modify"
      },
      "new_string": {
        "type": "string",
        "description": "The text to replace it with (must be different from old_string)"
      },
      "old_string": {
        "type": "string",
        "description": "The text to replace"
      },
      "replace_all": {
        "type": ["boolean", "null"],
        "description": "Replace all occurrences of old_string. Use null for the default false behavior."
      }
    },
    "additionalProperties": false
  }
