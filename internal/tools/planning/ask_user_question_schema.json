{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["header", "question", "options"],
  "additionalProperties": false,
  "properties": {
    "header": {
      "type": "string",
      "description": "Short label for the decision, at most 12 characters."
    },
    "question": {
      "type": "string",
      "description": "The single consequential question to ask the user."
    },
    "options": {
      "type": "array",
      "minItems": 2,
      "maxItems": 4,
      "description": "Concrete mutually exclusive options. Put the recommended option first and include '(Recommended)' in its label.",
      "items": {
        "type": "object",
        "required": ["label", "description"],
        "additionalProperties": false,
        "properties": {
          "label": {
            "type": "string",
            "description": "Short option label."
          },
          "description": {
            "type": "string",
            "description": "One sentence explaining the implication or tradeoff."
          }
        }
      }
    }
  }
}
