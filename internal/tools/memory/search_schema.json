{
    "type": "object",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "required": ["query", "scope", "limit", "tags"],
    "properties": {
        "query": {
            "type": "string",
            "description": "Search query for remembered thread, project, user, or global context. Search before proposing memory changes when a related memory may already exist."
        },
        "scope": {
            "type": "string",
            "enum": ["thread", "project", "user", "global", "all"],
            "description": "Memory scope to search. In a Discord thread, all defaults to thread-only memory."
        },
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 20,
            "description": "Maximum number of memories to return."
        },
        "tags": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Optional exact tags to filter results by. A memory must contain every listed tag."
        }
    },
    "additionalProperties": false
}
