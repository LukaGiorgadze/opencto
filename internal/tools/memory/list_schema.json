{
    "type": "object",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "required": ["scope", "kind", "tags", "limit"],
    "properties": {
        "scope": {
            "type": "string",
            "enum": ["project", "user", "global", "all"],
            "description": "Memory scope to list. User scope is limited to the current user."
        },
        "kind": {
            "type": "string",
            "description": "Optional exact memory kind filter. Use an empty string for all kinds."
        },
        "tags": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Optional exact tags to filter results by. A memory must contain every listed tag."
        },
        "limit": {
            "type": "integer",
            "minimum": 1,
            "maximum": 50,
            "description": "Maximum number of recent memories to return."
        }
    },
    "additionalProperties": false
}
