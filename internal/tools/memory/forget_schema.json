{
    "type": "object",
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "required": ["memory_ids", "tags", "scope"],
    "properties": {
        "memory_ids": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Delete by exact memory ids. Use an empty array when deleting by tags or scope. Search memory first if unknown."
        },
        "tags": {
            "type": "array",
            "items": {
                "type": "string"
            },
            "description": "Delete by tags. Use an empty array when deleting by ids or scope. Matching memories must contain all tags."
        },
        "scope": {
            "type": "string",
            "enum": ["project", "global", "all"],
            "description": "Delete by scope when memory_ids and tags are empty. Use all when deleting by memory_ids or tags."
        }
    },
    "additionalProperties": false
}
