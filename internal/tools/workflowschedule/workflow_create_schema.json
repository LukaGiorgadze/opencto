{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "required": ["workflow_id", "prompt"],
  "additionalProperties": false,
  "properties": {
    "workflow_id": {
      "type": "string",
      "minLength": 1,
      "description": "Stable workflow id. Source is authored under $OPENCTO_WORKSPACE/workflows/{workflow_id}."
    },
    "prompt": {
      "type": "string",
      "minLength": 1,
      "description": "Self-contained workflow authoring request. Include: goal description, trigger and schedule, step-by-step desired behavior, state rules and expected inputs and outputs, credential and service requirements, error handling expectations, and validation criteria."
    },
    "commit_message": {
      "type": "string",
      "description": "Commit message for the final authored workflow bundle."
    }
  }
}
